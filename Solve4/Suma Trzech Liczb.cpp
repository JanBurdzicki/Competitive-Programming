#include <bits/stdc++.h>
using namespace std;

int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	cout.tie(0);

	long long a, b, c;
	cin >> a >> b >> c;

	long long wynik = a + b + c;

	cout << wynik << "\n";
	return 0;
}